# GitHub Workflows

Planned for v1.1+:
- validate.yml — CSS/JS syntax check + 48-point audit on PR
- size.yml — warn if any file exceeds 50KB
- link-check.yml — daily check of all doc links
- release.yml — auto-generate release notes from CHANGELOG.md
